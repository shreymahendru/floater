name: floater
description: Flutter framework with helpers that floats on top of flutter to make flutter development simple.
version: 0.0.1
homepage: https://github.com/shreymahendru/floater

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  
  meta: ^1.1.8
  get_it: ^4.0.2
  flutter_secure_storage: ^3.3.2
  
dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
